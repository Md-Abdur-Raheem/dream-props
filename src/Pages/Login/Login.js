import { Typography } from '@mui/material';
import React from 'react';

const Login = () => {
    return (
        <div>
            <Typography
                variant="h3"
                sx={{marginTop: 10}}
            >I am from login</Typography>
        </div>
    );
};

export default Login;